import styled from 'styled-components';

const Wrapper = styled.div`
  width: 30rem;
  height: 30rem;
  background: orange;
`;

function ProductItem() {
  return <Wrapper>Product Item</Wrapper>;
}

export default ProductItem;
